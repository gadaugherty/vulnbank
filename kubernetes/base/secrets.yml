# NOTE: In production, use External Secrets Operator or Sealed Secrets.
# Plain Kubernetes secrets are base64 encoded, not encrypted.
# This file exists to show the pattern — real secrets should come from
# AWS Secrets Manager, HashiCorp Vault, or similar.
apiVersion: v1
kind: Secret
metadata:
  name: vulnbank-db-credentials
  namespace: vulnbank
type: Opaque
data:
  # echo -n "vulnbank_app" | base64
  username: dnVsbmJhbmtfYXBw
  # Placeholder — set via sealed-secrets or external-secrets in production
  password: REPLACE_WITH_SEALED_SECRET

---
apiVersion: v1
kind: Secret
metadata:
  name: vulnbank-jwt
  namespace: vulnbank
type: Opaque
data:
  # Placeholder — generate with: openssl rand -base64 32
  secret: REPLACE_WITH_SEALED_SECRET
